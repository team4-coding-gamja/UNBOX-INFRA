{{- if and .Values.rollout.enabled (eq .Values.rollout.strategy "canary") .Values.rollout.featureFlag.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-canary
  labels:
    app: {{ .Values.appName }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort | default 8080 }}
  selector:
    app: {{ .Values.appName }}
{{- end }}
