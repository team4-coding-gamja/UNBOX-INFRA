apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unbox-applications-dev
  namespace: argocd
  labels:
    type: application
    bundle: apps
    env: dev
  annotations:
    argocd.argoproj.io/sync-wave: "10"  # 인프라 이후 배포
spec:
  project: default
  source:
    repoURL: https://github.com/team4-coding-gamja/UNBOX-INFRA.git
    targetRevision: feat/ArgoCD-dev
    path: gitops/apps
    directory:
      recurse: true
      include: "*/application-dev.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  info:
    - name: 'Description'
      value: 'Dev Application services (User, Product, Order, Trade, Payment)'
    - name: 'Deployment Order'
      value: 'Wave 10: User → Wave 11: Product/Order/Payment → Wave 12: Trade'
    - name: 'Features'
      value: 'Product: Canary+FeatureFlag, Trade: Canary+Analysis+AutoRollback'
